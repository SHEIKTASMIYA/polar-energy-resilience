# components/controls/

Planned:
- RangeSelector.tsx       — 24h/7d/30d style horizon selector, re-queries via hooks (never
                            client-side slices a fixed dataset — see docs/08)
- ToggleGroup.tsx         — generic segmented control (used for dispatch strategy, chart metric toggle)
- ScenarioParamSlider.tsx — labeled slider + paired numeric input (docs/12 touch requirement)
- StationSelector.tsx     — dropdown driven by config/stations.ts

Not yet implemented.
